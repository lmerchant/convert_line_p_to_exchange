language: python
python:  
  - "3.7"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - python -m pytest
env:
  - PYTHONPATH=$PYTHONPATH:$TRAVIS_BUILD_DIR:$TRAVIS_BUILD_DIR/convert_line_p_to_exchange:.:$TRAVIS_BUILD_DIR/tests
