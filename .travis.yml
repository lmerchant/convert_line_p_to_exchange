language: python
python:  
  - "3.7"
# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install .
# command to run tests
script:
  - pytest
env:
  - PYTHONPATH=$PYTHONPATH:$TRAVIS_BUILD_DIR:$TRAVIS_BUILD_DIR/convert_line_p_to_exchange:./convert_line_p_to_exchange:./tests
